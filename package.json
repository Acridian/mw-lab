{
  "name": "mw-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && prettier --check . && astro build",
    "preview": "astro preview",
    "check": "astro check && prettier --check .",
    "verify": "npm run build && node scripts/verify-build.mjs",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/geologica": "^5.2.8",
    "@fontsource-variable/onest": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.8.3"
  }
}
